name: leaflog_app
description: leaflog app

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^4.2.0
  http: ^0.13.3
  logger: ^1.0.0
  url_launcher: ^6.0.4
  persistent_bottom_nav_bar: ^4.0.2

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true